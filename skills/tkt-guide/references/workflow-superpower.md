# Superpower 场景

没思路或重设计时使用。

## 安装方式

项目级 plugin，在 Claude Code 中运行：

```bash
/plugin install superpowers@claude-plugins-official
```

选择 "Install for you, in this repo only" 或 "project scope"。

## 使用方式

安装后 superpowers 会自动触发 brainstorming → executing-plans，不需要手动调用 skill。
